import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      "images.ray-ban.com",
      "images.oakley.com",
      "assets.vogue-eyewear.com",
      "assets.persol.com",
      "www.tomsteddy.com",
      "marketplace.canva.com",
      "www.emooptik.com.tr",
      "www.google.com"
    ],

  },
};

export default nextConfig;
